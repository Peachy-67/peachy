# HEARTBEAT.md

## Active tasks

- Check `/learning` for new files.
- For each new file:
  1. Read it fully.
  2. Write a concise summary.
  3. Extract key facts/decisions/todos.
  4. Save those facts to `memory/YYYY-MM-DD.md`.
  5. If long-term important, also update `MEMORY.md`.
- Avoid re-processing files already handled (track filenames + modified times).
