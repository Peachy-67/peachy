# Eval Facts 001

System facts for extraction test:
- The workspace has four operational areas: `tasks/`, `runs/`, `learning/`, and `scratch/`.
- Run logs are stored in `runs/YYYY-MM-DD_run.md`.
- Learning ingestion tracking is stored in `memory/learning-processed.json`.
- `MEMORY.md` is reserved for validated long-term knowledge.
